enum EventName {
    beforeSetup = 'stepper.before.setup',
    afterSetup = 'stepper.after.setup',

    beforeDestroy = 'stepper.before.destroy',
    afterDestroy = 'stepper.after.destroy',

    beforeReset = 'stepper.before.reset',
    afterReset = 'stepper.after.reset',

    beforeChange = 'stepper.before.change',
    afterChange = 'stepper.after.change',
}

export default EventName;